/*// Example test
/*describe('Dashboard Tests', () => {
  beforeEach(() => {
    // Call the login command with username and password
    cy.successfullogin()
    // Now Cypress is logged in, and the authentication token is stored
    cy.visit('https://account.africanmanagers.org/dashboard/home'); // Visit the dashboard page
  });

  it('should display the dashboard page', () => {
    // Add assertions to verify elements on the dashboard page
    cy.contains('AMI');
    cy.url().should('include', '/dashboard');
  });

  // Add more dashboard tests here...
}); */
